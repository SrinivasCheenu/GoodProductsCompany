@model IEnumerable<GoodProductsCompany.Models.Employee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmpName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EDOB)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmpMail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmpName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EDOB)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmpMail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
